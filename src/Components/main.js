// Sample array
const originalArray = [1, 2, 3, 4, 5];

// Using the map method to multiply each element by 2
const multipliedArray = originalArray.map((element) => element * 2);

// Output the original and multiplied arrays
console.log('Original Array:', originalArray);
console.log('Multiplied Array:', multipliedArray);
